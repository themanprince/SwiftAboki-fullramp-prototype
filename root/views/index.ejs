<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Transfer Money</title>
		<script async src="https://cdn.transak.com/js/sdk/1.4.1/transak.js"></script>
	</head>
	<body>
			<input name="sendCurr" id="sendCurr" type="text" placeholder="sender currency e.g. NGN, USD"/>
			<input name="recCurr" id="recCurr" type="text" placeholder="receiver currency e.g. USD, INR"/>
			<input name="amount"  id="amount" type="number" placeholder="amount(in sender currency)"/>
			<input name="accName"  id="accName" type="text" placeholder="account name"/>
			<input name="accNum" id="accNum" type="number" placeholder="account number"/>
			<button type="submit" id="sendBtn">Send</button>
		
		<% if(shouldShow) {%>
		<div style="position: relative; width: 500px; height: 80dvh; margin: auto; box-shadow: 0 0 15px #1461db; border-radius: 15px; overflow: hidden">
            <iframe
                id="transakIframe"
                src="https://global.transak.com/?<%= queryString %>"
                allow="camera;microphone;payment"
                style="height: 100%; width: 100%; border: none">
            </iframe>
        </div>
        <% } %>
		
		<script type="module" src="./javascripts/index.js"></script>
	</body>
</html>